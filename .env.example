# Used by Astro to generate canonical URLs + sitemap in production builds
SITE=https://vicky-blog-ochre.vercel.app

# GitHub OAuth App credentials (set these in Vercel → Project → Settings → Environment Variables)
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Optional
# GITHUB_OAUTH_SCOPE=repo
# OAUTH_STATE_SECRET=