name: (deprecated) Notion sync (use notion-sync.yml)

on:
  workflow_call:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deprecated workflow. Use .github/workflows/notion-sync.yml"
